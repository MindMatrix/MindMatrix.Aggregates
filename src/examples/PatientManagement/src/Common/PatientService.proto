syntax = "proto3";

option csharp_namespace = "PatientManagement";

import "Encounter.proto";

package PatientManagement;

service PatientService {
    rpc AdmitPatient(AdmitPatient2) returns (Encounter);
}

message AdmitPatient2 {
    string Id = 1;
}

